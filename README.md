# niu-cli

## Env vars

env.PCKM - set package manager (bun | node)

NIU_CLI_PROJECT_OUTPUT_DIR - absolute url to output bootstrapped applications

## Initial setup

run `niu auth` and follow instructions to authenticate on plasmic


